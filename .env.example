NODE_ENV=development
PORT=3000

# MySQL
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=root
DB_NAME=bdblendlap

# JWT
JWT_SECRET=cambia_este_secreto_en_produccion
JWT_EXPIRES_IN=24h

# Correo remitente (Gmail con contraseña de aplicación)
# El usuario puede registrarse con CUALQUIER correo; este es solo el remitente.
# https://myaccount.google.com/apppasswords
EMAIL_USER=blendlap2026@gmail.com
EMAIL_PASS=nweoejikopopefif
EMAIL_FROM="Blendlap Barbería <blendlap2026@gmail.com>"
# IMPORTANTE: EMAIL_FROM debe ir entre comillas si tiene espacios.

# Opcional: SMTP genérico (si defines SMTP_HOST, se usa en lugar de EMAIL_SERVICE)
# SMTP_HOST=smtp.tuproveedor.com
# SMTP_PORT=587
# SMTP_SECURE=false

# Ollama (chatbot)
OLLAMA_BASE_URL=http://ollama:11434
OLLAMA_MODEL=llama3.2:1b
